<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Elastic Beanstalk K-vs-K</title>
    <link rel="stylesheet" href="/assets/css/home.css">
</head>
<body>
 <h1>Soliman</h1>
    <!-- Uses built-in `if` helper. -->
  {{#if showTitle}}
    <h1>Home</h1>
  {{/if}}
 
    <!-- Calls `foo` helper, overridden at render-level. -->
    {{!--<p>{{foo}}</p>--}}
 
    <!-- Calls `bar` helper, defined at instance-level. -->
    {{!--<p>{{bar}}</p>--}}
 
</body>
</html>